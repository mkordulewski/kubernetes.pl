ErrorDocument 404 /404.html

RewriteEngine On

RewriteCond %{HTTP_HOST} ^(.+)\.(kubernetes\.pl)$ [NC]
RewriteRule ^ http://%2%{REQUEST_URI} [R=301,L,NE]

# force ssl
RewriteCond     %{SERVER_PORT} ^80$
RewriteRule     ^(.*)$ https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L,NE]